# Avoid hard failures on slightly older Node 20.x (e.g. 20.14) when a transitive
# package declares engines like eslint-visitor-keys (^20.19). Prefer upgrading
# Node to >=20.19 when possible: https://nodejs.org/
engine-strict=false
