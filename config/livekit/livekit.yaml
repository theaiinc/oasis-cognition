port: 7880
rtc:
  node_ip: 127.0.0.1
  tcp_port: 7881
  port_range_start: 50600
  port_range_end: 50700
  use_external_ip: false
  stun_servers: []
keys:
  # Default dev key — override via LIVEKIT_KEYS env var in docker-compose.
  # For production, set LIVEKIT_API_KEY and LIVEKIT_API_SECRET in .env.
  devkey: oasis-livekit-dev-secret-min-32-chars
logging:
  level: info
