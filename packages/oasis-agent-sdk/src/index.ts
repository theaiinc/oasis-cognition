/**
 * @theaiinc/oasis-agent — Oasis Agent SDK
 *
 * Typed client for the oasis-agent service (coordinator jobs, external agents,
 * dev-agent proxy). Used by the api-gateway and third-party integrations.
 */

export { OasisAgentClient } from './client';
export type { OasisAgentClientOptions } from './client';

export * from './types';
